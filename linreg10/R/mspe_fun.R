# Mean Square Prediction Error (MSPE)
# computed in matrix form (see the formula in the project.html file)

#parameter vector beta
# y = response
# p = number of predictors

mspe <- function(data , ) {
  
  
  # n is the number of observations in the data (number of rows)
  n = nrow(data)
  
  
  
  
  
  
  
  
  
  
  
}